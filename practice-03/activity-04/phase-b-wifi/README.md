# Phase B: WiFi Web Server

Place the ESP32 WiFi web server sketch and web assets here.

Expected implementation:

* connect the ESP32 to a WiFi network
* expose a small web server
* visualize sensor or component data from a browser
* control outputs from a device connected to the same network

Suggested sketch name:

* `wifi-web-server.ino`
