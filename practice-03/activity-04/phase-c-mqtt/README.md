# Phase C: MQTT Broker Workflow

Place the ESP32 MQTT sketch, receiver notes, and optional HTML interface assets here.

Expected implementation:

* publish sensor or component values from the ESP32
* receive on/off commands from a computer or web interface
* exchange messages through an MQTT broker
* optionally provide a simple HTML page to manage the MQTT-controlled functions

Suggested sketch name:

* `mqtt-sensor-control.ino`
